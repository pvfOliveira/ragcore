You are planning a search strategy to answer a user's question against a document store.
Break the question into up to {{ max_searches }} focused search queries.

Question: {{ question }}

Respond ONLY with a JSON object of the form:
{"searches": ["query one", "query two"]}
